TASK: Generate a DAILY SOCIAL CONTENT DRAFT for NEOX.

Context:
- This is a single-day content idea.
- The draft will be reviewed by the CEO in AI HQ.
- Output must be detailed and production-ready.

DATE: {{today}}
TENANT: {{tenantId}}

FORMAT RULE (STRICT):
The format is PRESELECTED by the system.

FORMAT = {{format}}

You MUST use this format.
Do NOT choose another format.

Allowed formats:
- image
- carousel
- reel

If FORMAT = image → produce single-frame content.
If FORMAT = carousel → produce 5–8 slides.
If FORMAT = reel → produce 3–6 scenes.

Brand:
NEOX is an AI automation and digital technology company.

Brand tone:
- premium
- modern
- intelligent
- innovation-focused
- confident but not arrogant

Content must:
- feel futuristic
- highlight AI, automation, business efficiency
- look like a high-end tech brand

Audience:
- startup founders
- SMEs
- tech founders
- entrepreneurs
- business owners interested in automation

Return STRICT JSON only.

JSON SCHEMA:

{
  "type": "content_draft",
  "tenantKey": "{{tenantId}}",
  "language": "az",
  "format": "{{format}}",
  "topic": "string",
  "goal": "lead|awareness|trust|offer",
  "targetAudience": "string",
  "hook": "string",
  "caption": "string",
  "cta": "string",
  "hashtags": ["#..."],
  "visualPlan": {
    "style": "string",
    "aspectRatio": "1:1|4:5|9:16",
    "composition": "string",
    "colorNotes": "string",
    "textOnVisual": ["string"],
    "frames": [
      {
        "index": 1,
        "frameType": "cover|slide|scene",
        "headline": "string",
        "subline": "string",
        "layout": "string",
        "visualElements": ["string"],
        "motion": "string",
        "durationSec": 0
      }
    ]
  },
  "assetBrief": {
    "neededAssets": ["image|video|icons|mockups"],
    "imagePrompt": "string",
    "brollIdeas": ["string"]
  },
  "complianceNotes": ["string"],
  "reviewQuestionsForCEO": ["string"]
}

Rules:

Caption:
- must sound natural
- must not sound robotic
- structure: hook → value → CTA
- max 1200 characters

Hashtags:
- between 8 and 18
- mix Azerbaijani and English
- include #AI #Automation #Neox

Carousel rules:
- 5 to 8 slides
- first slide must be a strong hook
- last slide must contain CTA

Reel rules:
- 3 to 6 scenes
- include motion
- include approximate duration

Image rules:
- single strong visual
- bold headline
- minimal text

Visual plan must clearly specify:
- where logo appears
- where headline appears
- layout guidance
- visual hierarchy

Asset brief must be practical:
- usable for designers
- usable for AI image tools

Return JSON only.
No explanation.
No markdown.
No text outside JSON.